<%= form_for([@post, @post.comments.build]) do |f| %>

     <!-- Form for inputing commenter name, comment and submission -->
     
	<%= f.label :name %><br>
	<%= f.text_field :name %><br>
	<br>
	
	<%= f.label :body %><br>
	<%= f.text_area :body %><br>
	<br>
	
	<%= f.submit %>
	
<% end %>